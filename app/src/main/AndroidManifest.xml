<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.azureobserver">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".myapplication.presentation.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AzureObserver">
        <activity
            android:name=".myapplication.presentation.activity.ServiceBusActivity"
            android:exported="false" />
        <activity
            android:name=".myapplication.presentation.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".myapplication.presentation.IotHubActivity"
            android:exported="true"></activity>
    </application>

</manifest>